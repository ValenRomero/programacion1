const valor = prompt("ingrese una calificacion")

const calificacion = ( valor >= 7) ? 'aprobado': 'desaprobado'
// temple string
console.log(`
       tu calificacion en el taller de
       programacion 1 es: ${valor}
`)
// se imprime en pantalla
document.write(valor)